package java.com.scaler.machinecoding.repositories;

import java.com.scaler.machinecoding.models.Gate;
import java.util.Optional;

/*Have every method allows to do CRUD onoperations on gate on DB  */
public class GateRepository {

    public Optional<Gate> findGateByID(Long id) {
        return Optional.empty();
    }
}
